CREATE TABLE `dj_info` (
                           `id` int(32) NOT NULL PRIMARY KEY,
                           `djname` varchar(64) NOT NULL,
                           `bio` varchar(4096) NOT NULL
);